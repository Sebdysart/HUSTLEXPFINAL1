/**
 * @format
 */

import React from 'react';
import { registerRootComponent } from 'expo';
import App from './App';

// Expo entrypoint (replaces AppRegistry for Expo-managed projects).
registerRootComponent(App);
